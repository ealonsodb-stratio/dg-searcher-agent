configuration.name="testIT_config"

extractor = {
  limit = 1000
  period.ms = 1000
  exponentialbackoff {
    pause.ms = 1000
    max.error.retry = 10
  }
  delay.ms = 0
}

source = {
  connection = {
    initial.size = 0
    max.size = 4
    url = "jdbc:postgresql://localhost:5432/governance"
    user = "postgres"
    password = "postgres"
  }
  database = "governance"
  schema = "dg_metadata"
  exponentialbackoff {
    pause.ms = 1000
    max.error.retry = 10
  }
}

indexer = {
  partition = 1000
}

searcher = {
  manager.url = "http://localhost:8080"
  indexer.url = "http://localhost:8082"
  model = "governance_search_test"
}

